# dependencias
node_modules/
.pnp
.pnp.js

# build
.next/
out/
build/
dist/

# entorno — NUNCA commitear claves
# Se ignora TODO lo que empiece por .env, en cualquier carpeta, y se
# rescatan los .example. Antes se listaban tres casos concretos y
# .env.development, por ejemplo, se colaba.
.env*
!.env*.example

# vercel / supabase
.vercel
.supabase/

# sistema
.DS_Store
Thumbs.db
*.log
npm-debug.log*

# tooling local
.claude/settings.local.json
